[project]
name = "santifi"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "Django>=5.1",
    "psycopg2-binary>=2.9",
]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"