{
  "buildCommand": "pip install -r requirements.txt && python manage.py collectstatic --noinput",
  "installCommand": "pip install -r requirements.txt",
  "framework": "django",
  "env": {
    "DJANGO_SETTINGS_MODULE": "financas.settings"
  }
}